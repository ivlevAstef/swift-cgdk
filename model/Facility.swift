public struct Facility {
	let id: Int64
	let type: FacilityType
	let ownerPlayerId: Int64
	let left: Double
	let top: Double
	let capturePoints: Double
	let vehicleType: VehicleType
	let productionProgress: Int
}
